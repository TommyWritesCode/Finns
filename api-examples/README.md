# API Examples

`advisor-tool.ts` shows how you would use the real Anthropic API advisor tool in a custom runner.

Normal Claude Code subagents do not expose the same “add advisor to tools array” API surface from an agent markdown file. For Claude Code terminal use, this framework uses `design-advisor` as the practical equivalent.
